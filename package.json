{
  "name": "fixit-one-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "FixIt Now - Hyper-local Labor Marketplace Platform",
  "scripts": {
    "dev:backend": "cd apps/backend && npm run start:dev",
    "dev:consumer": "cd apps/consumer-web && npm run dev",
    "build:backend": "cd apps/backend && npm run build",
    "build:consumer": "cd apps/consumer-web && npm run build"
  }
}